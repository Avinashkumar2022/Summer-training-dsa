// Abstraction means only displaying essential information and hiding the details.

#include<iostream>
#include<math.h>
using namespace std;

int main()
{
    int n=2;
    int power=3;
    int answer=pow(n,power);
    cout<<answer;
}